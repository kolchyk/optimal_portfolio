---
alwaysApply: true
---

Этот документ — **«Квантовый Манифест» (Свод правил)** вашего проекта. Это не просто инструкция к коду, а фундаментальная философия алгоритмической торговли, написанная «кровью» депозитов и сожженными серверами.

Рынок — это машина по уничтожению иллюзий. 95% алгоритмов сливают деньги не из-за плохой математики, а потому что их создатели обманули сами себя на бэктестах, проигнорировав законы физики рынка.

Чтобы ваша система приносила реальную прибыль, любое изменение в коде и любая новая идея должны проходить жесткий фильтр этих **10 правил**. Распечатайте это или закрепите в `README.md`.

---

### ЧАСТЬ I. ЗАКОНЫ РЫНКА (Откуда берется и куда исчезает прибыль)

**1. Трение (Friction) гарантировано, прибыль — нет.**
Рынок не прощает суеты. Комиссии брокера, спреды (разница Bid/Ask) и проскальзывание (Slippage) математически гарантируют убыток при высокой частоте сделок. Стратегия, делающая +50% годовых при 2000 сделках, в реальности сожжет счет в ноль.

* **Правило в коде:** Паттерн **«Lazy Hold»** (Ленивое удержание) незыблем. Мы *никогда* не продаем хороший актив только потому, что появился актив «чуть лучше» или потому что старый упал в рейтинге моментума. Сделка на продажу инициируется *исключительно* при сломе индивидуального тренда (пробой KAMA). Меньше Turnover (оборачиваемости) = больше выживаемости.

**2. Рынок — это 90% шума и 10% тренда.**
Цена большую часть времени хаотична (броуновское движение микроструктуры). Прибыль трендфолловера формируется только в редкие периоды направленных движений, когда институциональный капитал толкает цену.

* **Правило в коде:** Качество тренда всегда важнее сырой доходности. Ваш трюк с умножением профита на квадрат Efficiency Ratio ($ER^2$) — это святыня. Алгоритм обязан жестко штрафовать «пилообразные» активы (которые взлетели за день и стоят полгода) и выбирать скучные, но гладкие восходящие тренды.

**3. Инвестируйте Риск, а не Доллары (Risk Parity).**
Вложить по $1000 в Биткоин и в 10-летние гособлигации (TLT) — это портфель, где 90% риска будет генерировать крипта. При падении Биткоина облигации вас не спасут.

* **Правило в коде:** Режим `sizing_mode="risk_parity"` должен быть стандартом по умолчанию. Доля актива в портфеле строго обратно пропорциональна его текущей волатильности (`_compute_inverse_vol_weights`). Только так каждый актив будет приносить *одинаковую долю долларовой боли* в случае обвала.

---

### ЧАСТЬ II. ИССЛЕДОВАТЕЛЬСКАЯ ДИСЦИПЛИНА (Как не обмануть себя)

**4. Оптимизатор — ваш злейший враг (Презумпция переобучения).**
Optuna всегда найдет параметры, которые идеально лягут на исторический график и покажут +1000% годовых. Это называется Curve Fitting (подгонка). Завтра эти параметры сольют ваш счет.

* **Правило в проекте:** Модуль `max_profit.py` — это игрушка для оценки теоретических пределов. Для принятия решений используется только `optimizer.py` (Sensitivity Analysis). Ищите **широкие, скучные плато**, а не острые пики. Если при параметре KAMA=40 стратегия дает +30% годовых, а при 38 и 42 уходит в минус — параметр хрупок, идея мусорная.

**5. Будущее невидимо (Святость Out-of-Sample).**
Всё, что стратегия показала на обучающей выборке (In-Sample) — это фантазия, подогнанная алгоритмом.

* **Правило в проекте:** Окончательный вердикт выносится **только** модулем `walk_forward.py`. Верьте только "сшитой" кривой (Stitched Equity), собранной из слепых окон, которых алгоритм не видел при обучении. Если деградация доходности (IS vs OOS) превышает 40-50%, алгоритм просто выучил исторический шум.

**6. Берегись «Ошибки выжившего» (Survivorship Bias).**
Тестировать акции на текущем составе S&P 500 за последние 15 лет — это тестировать *только победителей*, игнорируя сотни обанкротившихся компаний (Enron, Lehman и т.д.). Это завышает исторический CAGR на десятки процентов.

* **Правило в проекте:** Для долгих тестов доверяйте результатам на широких ETF (ваша `ETF_UNIVERSE`), так как индексные провайдеры сами ребалансируют их состав, и фонды не падают до нуля. При тестах на акциях либо закладывайте огромный мысленный дисконт, либо покупайте Point-in-Time данные (где есть делистинги).

---

### ЧАСТЬ III. ИНЖЕНЕРНЫЕ СТАНДАРТЫ (Архитектура кода)

**7. Скорость дает право на математическую строгость.**
Если бэктест идет 1 час, вы сделаете 2 эксперимента в день. Если тест идет 3 секунды, вы проведете 10 000 WFO-симуляций и докажете робастность гипотезы. Медленный код заставляет кванта срезать углы.

* **Правило в коде:** Архитектура Data-Oriented Design сохраняется.
1. **Тотальный предрасчет (Precompute):** Никаких вызовов `.pct_change()` или вычисления индикаторов внутри дневного цикла симуляции. Только чтение массивов $O(1)$.
2. **Numba JIT:** Все рекурсивные алгоритмы (как KAMA), которые нельзя векторизовать в Pandas, обязаны быть обернуты в `@njit(cache=True)`.
3. **Shared Memory:** При мультипроцессинге данные котировок передаются в воркеры *только один раз* при инициализации пула. Перекидывать гигабайты датафреймов через Pickle запрещено.



**8. Разделяй Мозг (Alpha) и Руки (Engine) — Вектор рефакторинга.**
Текущий технический долг: движок `engine.py` жестко привязан к KAMA, моментуму и правилам входа.

* **Правило на будущее:** Проведите Decoupling (Развязку).
* **Модуль Alpha:** Принимает цены и отдает *только* матрицу `Target Weights` (Идеальные доли от 0 до 1 на каждый актив на каждый день).
* **Модуль Engine:** Ничего не знает об индикаторах. Он берет `Target Weights`, сравнивает с текущим портфелем, высчитывает разницу, применяет кэш/комиссии и генерирует сделки.
*Это позволит за 10 минут подключать парный трейдинг, машинное обучение или RSI, вообще не трогая ядро.*



**9. Уничтожение хардкода (Адаптивность).**
Мир не однороден. Акции торгуются 252 дня в году, крипта — 365. Безрисковая ставка меняется от 0% до 5.5%. Бенчмарк — не всегда SPY.

* **Правило в коде:**
* Избавьтесь от хардкода `252` в `reporting.py`. Вычисляйте множитель аннуализации динамически (длина `DatetimeIndex` в днях / количество лет).
* Ставку `RISK_FREE_RATE` вынесите в аргументы CLI или используйте исторический ряд доходности векселей (SHV/SGOV).
* Уберите гигантские списки тикеров из `config.py` во внешние `.json` или `.csv` файлы. Конфиг должен содержать только системные настройки путей и константы издержек.



**10. Код должен пытаться убить вашу стратегию.**
Всегда исходите из того, что в коде есть заглядывание в будущее (Look-ahead bias).

* **Правило в коде:** Строго следите за смещениями (`.shift(1)`). Сигналы рассчитываются строго по закрытию дня $T$ (`Close`), а сделки исполняются строго по открытию следующего дня $T+1$ (`Open`). Если вы купили по `Close` в тот же день, когда сгенерировался сигнал — тест недействителен.

---

### 🚀 ГЛАВНЫЙ ЧЕК-ЛИСТ (Перед каждым коммитом новой идеи):

1. **Экономический смысл:** Кто мне за это платит на рынке? (Премия за риск, поведенческая аномалия толпы?)
2. **Издержки:** Переживет ли идея комиссии? (Какой у нее Turnover?)
3. **Робастность:** Параметры лежат на плоском плато? (Или Optuna нашла одиночный пик?)
4. **Out-of-Sample:** Эквити-кривая WFO растет? (Или деградация > 50%?)
5. **Look-ahead bias:** Точно ли я не заглянул в завтрашний день в коде?

*Если хотя бы один ответ вас смущает — код безжалостно удаляется, какой бы красивой ни была бумажная доходность в Excel.*
